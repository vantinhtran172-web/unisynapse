// WIT Standalone Admin Operations Portal Configuration
window.WIT_ADMIN_CONFIG = {
  // Default Backend API endpoint
  DEFAULT_API_BASE: "http://localhost:8000/api/v1",
  
  // Default fallback key (if set via environment injection or manual entry)
  // In production/local deployment, key is stored safely in browser localStorage or prompt modal
  DEFAULT_KEY: "wit-admin-sec-9a8f4c2e1b7d5e3f01829475c8b6a12d",

  // Portal metadata
  PORTAL_NAME: "WIT Enterprise Admin Operations Portal",
  VERSION: "2.4.0-SECURE-ISOLATED"
};
